export default function NovaQuestaoPage({ params }: { params: { bancoId: string } }) {
  return (
    <section>
      <h1>Nova Questão</h1>
      {/* Combobox de banco, tipo, enunciado, peso, gabarito */}
    </section>
  );
}
