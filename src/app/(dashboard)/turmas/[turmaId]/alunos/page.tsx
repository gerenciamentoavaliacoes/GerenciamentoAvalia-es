export default function GerenciarAlunosPage({ params }: { params: { turmaId: string } }) {
  return (
    <section>
      <h1>Gerenciar Alunos</h1>
      {/* Lista de estudantes matriculados, remover/adicionar (nome, matrícula, e-mail) */}
    </section>
  );
}
