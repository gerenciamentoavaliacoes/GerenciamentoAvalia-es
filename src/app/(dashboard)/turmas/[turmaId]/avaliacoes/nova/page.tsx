export default function NovaAvaliacaoPage({ params }: { params: { turmaId: string } }) {
  return (
    <section>
      <h1>Nova Avaliação</h1>
      {/* Título, tipo, datas, participantes, montagem de questões, ordem fixa/aleatória */}
    </section>
  );
}
